block('kg-appbar')(
    js()(true),
    tag()('header'),
    elem('title').tag()('h1'),
    elem('panel')(
        tag()('nav'),
        wrap()(function(){
            return {
                elem: 'placeholder',
                content: this.ctx
            };
        })
    )
);
